services:
  runner:
    build: 
      context: .
      dockerfile: Dockerfile
    restart: always
    environment:
      - REPO=OpenTix/platform
      - TOKEN=<Github PAT token>
    deploy:
      resources:
        reservations:
          cpus: '2'
          memory: 256M
