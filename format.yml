services:
  runner:
    build: 
      context: .
      dockerfile: Dockerfile
    restart: always
    environment:
      - TOKEN=<Github PAT token>
      - ORG=OpentTix
    deploy:
      resources:
        reservations:
          cpus: '2'
          memory: 256M
